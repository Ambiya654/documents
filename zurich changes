Theek hai, step by step debug karte hain!
Yeh queries ek ek karke chalao:

Query 1 — Basic data check:
sqlSELECT COUNT(*) as TotalRecords
FROM audit_inputsummary_v2
WHERE Delete_Flag = 'N'
AND IsMoved = 'Y'

Query 2 — Date range check:
sqlSELECT COUNT(*) as TotalRecords,
MIN(Audit_Date) as MinDate,
MAX(Audit_Date) as MaxDate
FROM audit_inputsummary_v2
WHERE Delete_Flag = 'N'
AND IsMoved = 'Y'

Query 3 — Emp_Master aur userlist join check:
sqlSELECT TOP 5 
    b.Cmb_hreid,
    b.Auditor_Id,
    x.fname,
    x.lname
FROM audit_inputsummary_v2 b
JOIN Emp_Master x ON b.Auditor_Id = x.hreid
WHERE b.Delete_Flag = 'N'
AND b.IsMoved = 'Y'

Teeno ka result share karo — tab exact problem pakad mein aayegi! 😊
